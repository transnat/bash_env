syntax on

" Only do this part when compiled with support for autocommands.
if has("autocmd")
    " Use filetype detection and file-based automatic indenting.
    filetype plugin indent on

    " Use actual tab chars in Makefiles.
    autocmd FileType make set tabstop=2 shiftwidth=8 softtabstop=0 expandtab
endif

syntax enable

set tabstop=2         " The width of a TAB is set to 2.
set expandtab					" Expand tabs to spaces.
set shiftwidth=2      " Indents will have a width of 2.
set softtabstop=2     " Sets the number of columns for a TAB.
set number            " Put line numbers.
set spelllang=en_us   " Set spellcheck language.
set foldmethod=indent " Set fold for indented lines.

au BufNewFile, BufRead *.py " Set Python settings
    \ set tabstop=4 |
    \ set softtabstop=4 |
    \ set shiftwidth=4 |
    \ set textwidth=79 |
    \ set expandtab |
    \ set autoindent |
    \ set fileformat=unix
