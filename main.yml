---
- name: 'Build end-user Linux system.'
  gather_facts: true                                                        # Gather facts.
  hosts: 'localhost'
  roles:                                                                    # Order matters.
      - 'security'
      - 'performance_tweaks'
      - 'packages'
      - 'environment'
...
