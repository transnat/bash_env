---
- name: 'Security tasks.'
  tags:
      - 'security'
  ansible.builtin.include_tasks: '{{ item }}'
  loop:
      - 'kernel.yml'
      - 'filesystem.yml'
      - 'priviledge_escalation.yml'
      - 'ulimit.yml'
      - 'auditd.yml'
      - 'ssh.yml'
      - 'firewalld.yml'
...
